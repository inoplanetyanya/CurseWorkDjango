$(function() {
  // let maxWidth = 0;
  // $('.center-part').map(function() { if ($(this).width() > maxWidth) maxWidth = $(this).width() });
  // $('.center-part').map(function() {$(this).width(maxWidth + 10)})
});